# Iteration Cycles

Workflows support multi-pass refinement loops.
State checkpoints (`checkpoints.sqlite`) allow the pipeline to pause, accept user feedback, and rerun specific department steps without losing prior context.
